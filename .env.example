# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="your_gemini_api_key_if_used"

# APP_URL: The URL where this applet is hosted.
APP_URL="http://localhost:3000"

# MongoDB connection string - MUST BE FILLED
# Based on your credentials, your connection string should look like this:
MONGODB_URI="mongodb+srv://mirishfaqahmad905:mirishfaqahmad905@cluster0.utdlxdg.mongodb.net/globalscholarship?retryWrites=true&w=majority&appName=Cluster0"

# JWT Secret for admin authentication
JWT_SECRET="generate_a_secure_random_string"

# SMTP settings for sending password reset emails
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="true"
SMTP_USER="techhub905@gmail.com"
SMTP_PASS="AAshfAAq123@"
